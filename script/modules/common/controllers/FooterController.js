define(['app'], function(app)
{
    app.register.controller('FooterController',
    [
        '$scope', '$rootScope', '$cookieStore', '$state',
        function($scope, $rootScope, $cookieStore, $state) {
            // TODO
        }
    ]);
});