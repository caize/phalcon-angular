define(['app'], function(app)
{
    app.register.controller('HeaderController',
    [
        '$scope', '$rootScope', '$cookieStore', '$state', 'Authentical',
        function($scope, $rootScope, $cookieStore, $state, Authentical) {
        }
    ]);
});